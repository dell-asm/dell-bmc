source 'https://rubygems.org'

puppetversion = ENV.key?('PUPPET_VERSION') ? "= #{ENV['PUPPET_VERSION']}" : ['>= 5.3.3']
gem 'puppet', puppetversion
gem 'puppetlabs_spec_helper', :require => flase
gem 'puppet-lint', '>= 0.3.2'
gem 'facter', '~> 2.0'
